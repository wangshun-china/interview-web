services:
  portfolio:
    image: ${INTERVIEW_IMAGE:?set INTERVIEW_IMAGE}
    container_name: wangshun-portfolio
    restart: always
    ports:
      - "80:80"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
